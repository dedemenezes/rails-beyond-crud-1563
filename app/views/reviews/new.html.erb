<h1>Add your review to <%= @restaurant.name %></h1>

<%# <form action="/restaurants/12/reviews"></form> %>
<%= simple_form_for([@restaurant, @review]) do |f| %>
  <%= f.input :content %>
  <%= f.submit "Add review", class: 'btn btn-success' %>
<% end %>
